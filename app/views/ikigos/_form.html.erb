<%= form_with(model: ikigo, local: true) do |form| %>
  <% if ikigo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ikigo.errors.count, "error") %> prohibited this ikigo from being saved:</h2>

      <ul>
      <% ikigo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :ikigo_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
